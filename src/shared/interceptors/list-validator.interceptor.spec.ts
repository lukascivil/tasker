import { ListValidatorInterceptor } from './list-validator.interceptor';

describe('ListValidatorInterceptor', () => {
  it('should be defined', () => {
    expect(new ListValidatorInterceptor()).toBeDefined();
  });
});
